version: 2.1

orbs:
  node: circleci/node@5.2.0

executors:
  default:
    docker:
      - image: cimg/node@sha256:a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2

jobs:
  test:
    executor: default
    steps:
      - checkout
      - node/install-packages
      - run:
          name: Run tests
          command: npm test
      - store_test_results:
          path: test-results

  lint:
    executor: default
    steps:
      - checkout
      - run:
          name: Lint
          command: npm run lint

workflows:
  ci:
    jobs:
      - test
      - lint
