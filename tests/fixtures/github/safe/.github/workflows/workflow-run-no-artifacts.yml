name: Notify on completion
on:
  workflow_run:
    workflows: ["Build"]
    types: [completed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build ${{ github.event.workflow_run.conclusion }}"
