name: Greet
description: Greet a user
inputs:
  name:
    description: Name to greet
    required: true
runs:
  using: composite
  steps:
    - run: echo "Hello"
      shell: bash
      env:
        NAME: ${{ inputs.name }}
